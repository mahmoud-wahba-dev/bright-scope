// Small i18n helper config — keep paths and defaults here so other modules can import them
const i18nConfig = {
  localesPath: "/src/locales",
  defaultNamespace: "translation",
};

export default i18nConfig;
