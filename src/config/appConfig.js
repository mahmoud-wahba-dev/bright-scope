// General application-level configuration
const appConfig = {
  siteName: "Bright Scope",
  defaultLanguage: "en",
  supportedLanguages: ["en", "ar"],
  // Common phone placeholder that other parts can use if staticData is missing
  defaultPhone: "+971 55 444 5555",
};

export default appConfig;
