# Copy this file to `.env` (or create a `.env.local`) and update the URL to point to your backend API.
# Recommended variable name used by the project: VITE_API_BASE_URL
# Example: if your API in Postman points to https://api.example.com/api/v1
# then set the value below to that full base path.

VITE_API_BASE_URL=https://api.example.com/api/v1

# Alternative name supported (backwards/other convention):
# VITE_API_URL=https://api.example.com/api/v1
